<?php

namespace App\Services;

/**
 * Временный отказ Gemini: есть смысл повторить позже.
 *
 * Отделено от «ответ не по схеме» и «неверный ключ» намеренно. Первое лечится
 * повторной попыткой из очереди, второе — нет, и гонять такое по кругу значит
 * пять раз подождать впустую и всё равно уехать в expenses.dead.
 *
 * До появления очереди разницы не было: {@see GeminiClassifier::classify()}
 * возвращал пустой массив на любую ошибку, потому что повторить всё равно
 * было негде.
 */
class GeminiUnavailable extends \RuntimeException
{
}
