# Шаблон корневого config.txt. Скопируйте в config.txt и впишите свои токены.
#
# Этот файл читает только start-docker.bat — он переносит оба значения в .env,
# откуда их берёт docker-compose. Больше корневой config.txt нигде не используется.
#
# Не путайте с php/config.txt: тот генерирует контейнер init-script на каждом
# `docker compose up` (CLASSIFIER_URL, DASHBOARD_URL, TELEGRAM_BOT_TOKEN),
# и ручные правки в нём не сохраняются.

TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
NGROK_AUTHTOKEN=your_ngrok_auth_token_here
